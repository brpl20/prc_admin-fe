const roleSubjectAccess: Record<string, string[]> = {
  accountant: ['tributary', 'tributary_pis'],
  default: ['civel', 'social_security', 'laborite', 'others'],
};

export default roleSubjectAccess;
